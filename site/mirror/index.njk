---
layout: base.njk
title: The Inverse Mirror
description: Reveals the gap between what you perceive and what the system is actually doing.
---
<div class="path-start" data-path="mirror">
  <h1>{{ paths.mirror.name }}</h1>
  <p class="path-hook">{{ paths.mirror.hook }}</p>
  <p>{{ paths.mirror.description }}</p>

  <p class="duration">{{ paths.mirror.duration }}</p>

  <nav class="start-nav">
    <a href="/mirror/1/" class="btn start">Begin</a>
  </nav>
</div>
